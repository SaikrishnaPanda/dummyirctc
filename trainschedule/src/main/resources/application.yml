server:
  port: '9090'
spring:
  application:
    name: trainschedule
  datasource:
    password: admin
    url: jdbc:postgresql://localhost:5432/dummyirctc?createDatabaseIfNotExist=true
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect